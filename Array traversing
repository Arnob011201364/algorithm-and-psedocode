int a[50],size,i;
printf(enter size of array);
scanf("%d",&size);
printf("enter element of an array");
for(i to size-1){//index start from zero
scanf("%d",&a[i]);
}
for(i to size -1){
printf(a[i]);
}
